# エージェント共通ルール

このリポジトリは技術ブログの検証と執筆を行うプロジェクトです。

## プロジェクト構造

```
project.yaml    # プロジェクトのメタデータ・状態管理
NOTES.md        # 検証ノート（全ての記録はここに集約）
poc/            # 検証コード
blog/           # ブログ記事（記事ごとにサブディレクトリ）
```

## セッション開始時

新しいセッションを開始したら、まず以下を確認してください:

1. `project.yaml` で現在のフェーズと記事の状態を把握する
2. `NOTES.md` で検証の進捗と方針を把握する
3. `poc/` のコードで検証の現状を把握する

これにより、前のセッションからの作業をスムーズに引き継げます。

## 検証記録のルール

検証作業中は、結果を `NOTES.md` に時系列で追記してください。

### 何を記録するか

コードを読んだだけではわからない情報を重点的に記録してください:

- なぜその方針・技術を選んだか
- 失敗したアプローチとその理由
- ハマりポイントと解決方法
- 想定と異なった挙動
- ユーザーが「ブログに書きたい」と言ったこと

### 記録のフォーマット

```markdown
### YYYY-MM-DD: 見出し
- やったこと、わかったこと
- 失敗したこと -> 原因
- -> ブログ: 記事に書きたいポイント
```

`-> ブログ:` マーカーで、記事執筆時に拾うべきポイントを示してください。

### 記録しなくてよいもの

- コードを読めばわかること
- 些末な作業ログ（ファイル作成、依存追加など）

## コミットルール

- 検証コードのコミットは通常通り行ってください
- `NOTES.md` と `project.yaml` の更新もコミットに含めてください
